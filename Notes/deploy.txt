Deploy

Jenkins

UGANDA-CUSTOMIZATION

Left side configuration
Set branch name

Apply and save

Build now

Jenkins / TT079 / deploy / env (dev2) / all /
Left side Build with Parameters

Modificar versiones:
	DMS_DB_VERSION
	DMS_RPM_VERSION
	WEBAPP_RPM_VERSION
	WEBAPP_WEBSELF_VERSION
	WEBAPPUNBS_RPM_VERSION
	WEBAPPUNBS_WEBSSO_VERSION
3.1.0-SNAPSHOT

Para reiniciar DB:
	CLEAN_DB, LOADTESTDATA, LOADEXAMPLEDATA
